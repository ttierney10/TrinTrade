export const search_box = {
    marginTop: "20px",
    marginLeft: "10%",
    width: "45%",
    display: "inline-block"
}
export const items_box = {
    marginTop: "20px",
    marginLeft: "5%",
    marginBottom: "600px",
    width: "60%",
    borderStyle: "solid",
    display: "inline-block"
}
export const filters_box = {
    display: "inline-block",
    borderStyle: "solid",
    width: "175px",
    cssFloat: "right",
    marginTop: "20px"
}
/*
function setInput(){
    if(bool){
        let max = 0
        if(searchInput){
            _.map(allSearchItems, (item) => {
                let price = item.price
                if(price > max){max = price}
            })
        }
        else{
            _.map(allItems, (item) => {
                let price = item.price
                if(price > max){max = price}
            })
        }
        console.log(max)
        setBool(false)
        setMaxDefault(max)
    }
    return false
}*/